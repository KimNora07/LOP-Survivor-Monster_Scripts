public class MonsterAnimationName
{
    public const string isRun = nameof(isRun);
    public const string isDeath = nameof(isDeath);
    public const string isAttack = nameof(isAttack);
    public const string isGetHit = nameof(isGetHit);
}
